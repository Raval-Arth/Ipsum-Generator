const test = () => {
  //   let words = [];
  //   words.push([data[Math.floor(Math.random() * data.length)]]);
  // for (let index = 0; index < emoji.length; index++) {
  //   //   const element = array[index];
  //   if (emoji[index].category == "Travel & Places") {
  //     emojis += emoji[index].emoji;
  //     emojis += "";
  //     console.log(emoji[index].description);
  //   }
  // }
};
